/target

# tooling artifacts (skill-registry cache), not part of the app
.atl/

# git worktree copies created for parallel sessions
.worktrees/


# Working notes/audits kept out of the repo (see the file header).
/TODO*.md
